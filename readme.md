# Лабораторные работы по операционным системам (2 сем)

На 7 можно было и остановиться... 

## Содержание

| Лабораторная | Материалы |
| --- | --- |
| 1 | Локальные сокеты: `socketpair`, `AF_UNIX`/`SOCK_DGRAM`, клиент и сервер вычислений. |
| 2 | TCP-клиент и многопользовательские TCP-серверы: варианты с `fork()` и потоками. |
| 022 | TCP-клиент и сервер с обработкой нескольких соединений. |
| 3 | Обход дерева каталогов и работа с потоками каталогов. |
| 4 | Получение и вывод информации о процессе через `/proc`. |
| 5 | Модуль ядра, выводящий информацию о задачах (`task_struct`). |
| 6 | Взаимодействие модулей ядра через экспортируемые символы. |
| 7 | Модуль `/proc` для получения информации о `task_struct` по PID. |
| 8 | Файл `/proc` с интерфейсом `seq_file`. |
| 9 | Файл `/proc` с интерфейсом `single_open`. |
| 10 | Обработка клавиатурного прерывания и tasklet. |
| 11 | Обработка клавиатурного прерывания и очередь работ. |
| 12 | Ввод-вывод из нескольких потоков; варианты с `fopen`, `append` и mutex. |
| 13 | Простая VFS с выделением объектов через slab cache. |
| IPQ | Вопросы и ответы по tasklet, softirq и очередям работ: [общий конспект](lab_ipq/README.md). |

## Лабораторная 1

Исходники находятся в [lab_01](lab_01): части с `socketpair`, датаграммными локальными сокетами и клиентом/сервером арифметических операций.

## Лабораторная 2

Исходники находятся в [lab_02](lab_02): TCP-клиент, сервер с `fork()` и серверы с потоками.

## Лабораторная 022

Исходники находятся в [lab_022](lab_022): TCP-клиент и TCP-сервер.

## Лабораторная 3

Исходники находятся в [lab_03](lab_03): рекурсивный обход каталогов.

### Подсказки и ответы

При достижении конца потока каталогов возвращает Null

К файлу по короткому имени можно обращаться в текущей рабочей категории

Когда создается новый процесс

## Лабораторная 4

Исходники находятся в [lab_04](lab_04): чтение и вывод информации о процессе из `/proc`.

## Лабораторная 5

Исходники находятся в [lab_05](lab_05): модуль ядра для вывода информации о задачах.

### Подсказки и ответы

Флаг Fork_no_exec - не может выполняться переход на другое адресное пространство
No frees не может блокироваться
kthread daemon 

start, stop, .. не являются точками входа 

какая функция выполняет основную работу 

Системный вызов fork() должен предоставить процессу-потомку логически идентичную копию адресного пространства его родителя. В большинстве случаев потомок заменяет предоставленное адресное пространство, так как сразу же после выполнения fork вызывает exec или exit. Таким образом, создание копии адресного пространства (так, как это реализовано в первых системах UNIX) является не оптимальной процедурой. 
Вышеописанная проблема была решена двумя различными способами. Сначала был разработан метод копирования при записи (copy-on-write), впервые реализованный в ОС System V и в настоящий момент используемый в большинстве систем UNIX и LINUX.

Чаще всего нет смысла в выполнении двух одинаковых процессов и потомок сразу выполняет системный вызов exec(), параметрами которого является имя исполняемого файла и, если нужно, параметры, которые будут переданы этой программе. 

Говорят, что системный вызов exec() создает низкоуровневый процесс: создаются таблицы страниц для адресного пространства программы, указанной в exec(), но программа на выполнение не запускается, так как это не полноценный процесс, имеющий идентификатор и дескриптор. Системный вызов exec() создает таблицу страниц для адресного пространства программы, переданной ему в качестве параметра, а затем заменяет старый адрес новой таблицы страниц.

Fork_no_exec Запрещает переход на новое адресноое пространство - вызов exec

## Лабораторная 6

Исходники находятся в [lab_06](lab_06): модули `my_md1`, `my_md2` и `my_md3`, демонстрирующие экспорт и использование символов.

## Лабораторная 7

Исходники находятся в [lab_07](lab_07): `/proc`-интерфейс для информации о процессе по PID.

## Лабораторная 8

Исходники находятся в [lab_08](lab_08): `/proc`-интерфейс на основе `seq_file`.

## Лабораторная 9

Исходники находятся в [lab_09](lab_09): `/proc`-интерфейс на основе `single_open`.

## Лабораторная 10

Исходники находятся в [lab_10](lab_10): tasklet, обработчик клавиатурного прерывания и файл `/proc/keyboard`.

## Лабораторная 11

Исходники находятся в [lab_11](lab_11): очередь работ, обработчик клавиатурного прерывания и файл `/proc/key_buf_wq`.

## Лабораторная 12

Исходники и отчёт находятся в [lab_12](lab_12): исследование файлового ввода-вывода при работе нескольких потоков.

## Лабораторная 13

Исходники находятся в [lab_13](lab_13): модуль простой файловой системы `my_vfs` со slab cache.

## IPQ

Полный общий конспект вопросов и ответов расположен в [lab_ipq/README.md](lab_ipq/README.md). Тематические варианты: [tasklet](lab_ipq/tasklet.md) и [wq](lab_ipq/workq.md).
